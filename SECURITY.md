# 安全与隐私说明（Security & Privacy）

## 数据边界（最重要）

- **一切数据仅存本机**：书稿项目、世界观设定、正文、连续性档案、模型厂商配置与
  **API Key** 均保存在运行目录的 `data/` 下（`projects/*.json`、`settings.json`）。
- **无遥测、无上报**：本程序不包含任何统计、崩溃上传或远程调用（唯一的网络请求
  是你配置的模型 API 与可选的开源镜像下载）。
- 默认只监听 `127.0.0.1`；如需局域网访问请显式把 `server.bind` 改为 `0.0.0.0`，
  此时同一局域网内任何人都可能访问到你的 Key 与书稿——请自行评估风险。

## API Key 的存放与建议

- Key 以**明文 JSON** 存在本机 `data/settings.json`。这是本地个人工具的可预期行为，
  但请注意：不要把 `data/` 目录随 zip 分发、备份到不受信网盘或提交进 git。
- 建议：① 使用专用的、可随时吊销的 API Key；② 定期备份 `data/` 到加密介质；
  ③ 若在多台机器使用，各自配置独立 Key。

## 内容与版权

- 工具产出的文本（含"模拟引擎"演示文）与用户创作内容的所有权归属用户；
  本项目不收集、不主张任何创作内容。
- 内置示例《雾港来信》为本项目原创演示内容，随 MIT 许可分发。

## 依赖与供应链

- 零第三方运行时依赖（纯 Node.js 标准库），无 npm 安装环节，从源头降低供应链风险。
- 如从第三方镜像站获取发布包，请核对 `SHA-256`（发布包内附 `SHA256SUMS.txt`）。

## 上报渠道

发现安全问题请**不要**公开讨论细节：先在仓库开 private issue 或直接邮件联系维护者
（占位：见仓库主页联系方式）。报告时请附：版本号、复现步骤、影响面。

## 大陆用户提示

GitHub 访问受限时，请从你的镜像/网盘分发的 zip 安装，并核对校验和；
Node.js 可用国内镜像（如 nodejs.cn / npmmirror）下载。
